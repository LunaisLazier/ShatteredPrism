name: Build Application and Make Release

on:
 workflow_dispatch:
  push:
    tags:
      - "*"

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    with:
      build_type: Release
      is_qt_cached: false
    secrets:
      SPARKLE_ED25519_KEY: ${{ secrets.SPARKLE_ED25519_KEY }}
      WINDOWS_CODESIGN_CERT: ${{ secrets.WINDOWS_CODESIGN_CERT }}
      WINDOWS_CODESIGN_PASSWORD: ${{ secrets.WINDOWS_CODESIGN_PASSWORD }}
      APPLE_CODESIGN_CERT: ${{ secrets.APPLE_CODESIGN_CERT }}
      APPLE_CODESIGN_PASSWORD: ${{ secrets.APPLE_CODESIGN_PASSWORD }}
      APPLE_CODESIGN_ID: ${{ secrets.APPLE_CODESIGN_ID }}
      APPLE_NOTARIZE_APPLE_ID: ${{ secrets.APPLE_NOTARIZE_APPLE_ID }}
      APPLE_NOTARIZE_TEAM_ID: ${{ secrets.APPLE_NOTARIZE_TEAM_ID }}
      APPLE_NOTARIZE_PASSWORD: ${{ secrets.APPLE_NOTARIZE_PASSWORD }}
      GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
      GPG_PRIVATE_KEY_ID: ${{ secrets.GPG_PRIVATE_KEY_ID }}

  create_release:
    needs: build_release
    runs-on: ubuntu-latest
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: "true"
          path: "FracturedPrism-source"
      - name: Download artifacts
        uses: actions/download-artifact@v4
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/FracturedPrism-source FracturedPrism-${{ env.VERSION }}
          mv FracturedPrism-Linux-Qt6-Portable*/FracturedPrism-portable.tar.gz FracturedPrism-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv FracturedPrism-Linux-Qt5-Portable*/FracturedPrism-portable.tar.gz FracturedPrism-Linux-Qt5-Portable-${{ env.VERSION }}.tar.gz
          mv FracturedPrism-*.AppImage/FracturedPrism-*.AppImage FracturedPrism-Linux-x86_64.AppImage
          mv FracturedPrism-*.AppImage.zsync/FracturedPrism-*.AppImage.zsync FracturedPrism-Linux-x86_64.AppImage.zsync
          mv FracturedPrism-macOS-Legacy*/FracturedPrism.zip FracturedPrism-macOS-Legacy-${{ env.VERSION }}.zip
          mv FracturedPrism-macOS*/FracturedPrism.zip FracturedPrism-macOS-${{ env.VERSION }}.zip

          tar --exclude='.git' -czf FracturedPrism-${{ env.VERSION }}.tar.gz FracturedPrism-${{ env.VERSION }}

          for d in FracturedPrism-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="FracturedPrism-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv FracturedPrism-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in FracturedPrism-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="FracturedPrism-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv FracturedPrism-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: Fractured Prism ${{ env.VERSION }}
          draft: true
          prerelease: false
          files: |
            FracturedPrism-Linux-Qt5-Portable-${{ env.VERSION }}.tar.gz
            FracturedPrism-Linux-x86_64.AppImage
            FracturedPrism-Linux-x86_64.AppImage.zsync
            FracturedPrism-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            FracturedPrism-Windows-MinGW-w64-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            FracturedPrism-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            FracturedPrism-Windows-MSVC-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            FracturedPrism-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            FracturedPrism-macOS-${{ env.VERSION }}.zip
            FracturedPrism-macOS-Legacy-${{ env.VERSION }}.zip
            FracturedPrism-${{ env.VERSION }}.tar.gz
